#include <iostream>
using namespace std;

int main() {
    int numero = 5;

    cout << "Tabuada do número " << numero << ":" << endl;

    
    for (int i = 1; i <= 10; i++) {
        cout << numero << " x " << i << " = " << numero * i << endl;
    }

    return 0;
}
